import { ConvertirMayusPipe } from './convertir-mayus.pipe';

describe('ConvertirMayusPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirMayusPipe();
    expect(pipe).toBeTruthy();
  });
});
