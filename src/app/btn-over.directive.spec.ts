import { BtnOverDirective } from './btn-over.directive';

describe('BtnOverDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnOverDirective();
    expect(directive).toBeTruthy();
  });
});
